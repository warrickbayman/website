# Laravel Model Presenter

IsPresentable is a package designed to aid in associating presentation data to Laravel models. Since I work a lot on
both backend and frontend components, it made sense to be able to pass model attributes to the frontend without neding
to build more complex APIs. IsPresentable is one of the first open source packages I ever built and I still use it today.

It's really simple, but does the trick.
